# Keeps internal/web/frontend/dist present so //go:embed all:frontend/dist
# compiles when the SPA hasn't been built yet (e.g. a Go-only checkout or
# `go install`). The real bundle is generated by `make web-build` and is
# gitignored; at runtime a missing bundle degrades gracefully (server.go).
