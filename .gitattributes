# Generated TypeScript types mirrored from the Go wire contract by tygo. Committed
# (so the SPA's type-check has them) but marked generated so GitHub collapses them
# in PR diffs and drops them from the repo's language stats. Regenerate with
# `make web-types`. (The built web bundle, internal/web/frontend/dist, is no
# longer committed — it's generated at build time; see .gitignore.)
internal/web/frontend/src/lib/api-types.ts linguist-generated=true
