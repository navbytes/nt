# compiled binary
/nt
/nt-e2e

# goreleaser snapshot/release output (the embedded SPA dist/ lives under internal/)
/dist/

# editor / OS cruft
*.swp
.DS_Store

# go
*.test
*.out

# Claude Code scheduler runtime lock (leaks session id/pid — never track)
.claude/scheduled_tasks.lock
